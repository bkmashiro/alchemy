// src/dashboard/server.ts
// NOTE: Full implementation is owned by Agent C. This is a stub.

import { JobRegistry } from '../core/registry.js';

export async function createDashboardServer(
  _registry: JobRegistry,
  port: number,
): Promise<void> {
  console.log(`Dashboard server stub — port ${port}`);
  console.log('Full implementation pending Agent C.');
}
