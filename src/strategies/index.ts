// src/strategies/index.ts — barrel + self-registration
// NOTE: Full implementations are owned by Agent B.
// This file is a placeholder so the CLI entry point import doesn't fail.

export * from './base.js';
